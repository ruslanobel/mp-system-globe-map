# Архитектура проекта: Minimalist Mapbox Globe

## High-Level Overview

Это минималистичное веб-приложение для отображения интерактивного глобуса на базе Mapbox GL JS. Проект представляет собой простую статическую страницу с кастомным стилем карты в серых тонах, предназначенную для визуализации географических данных в чистом и лаконичном виде. Приложение использует Mapbox API для рендеринга векторной карты с настраиваемыми слоями (фон, вода, границы стран).

## Core Components

### 1. **Mapbox Visualization Layer**
- **Ответственность**: Инициализация и отображение интерактивной карты через Mapbox GL JS (v3.1.2)
- **Расположение**: `script.js` (referenced but missing - карта инициализируется здесь)
- **Функционал**: 
  - Создание экземпляра карты
  - Применение кастомного стиля (`mapbox_style.json`)
  - Обработка взаимодействия пользователя с картой

### 2. **Custom Map Style**
- **Ответственность**: Определение визуального стиля карты (цвета, слои, источники данных)
- **Расположение**: `mapbox_style.json`
- **Компоненты**:
  - Background layer (#BFBFBF - серый фон)
  - Water layer (#DFDFDF - светло-серая вода)
  - Admin boundaries (границы стран с толщиной линии 2px)

### 3. **UI Layer**
- **Ответственность**: Базовый HTML контейнер и стилизация страницы
- **Расположение**: `index.html`, `style.css`
- **Функционал**: Fullscreen container для карты с прозрачным фоном

## Data Flow

```
User Interaction
       ↓
   Browser DOM (#map container)
       ↓
   Mapbox GL JS Library (v3.1.2)
       ↓
   Custom Style (mapbox_style.json)
       ↓
   Mapbox Vector Tiles API
   (mapbox://mapbox.mapbox-streets-v8)
       ↓
   Rendered Map (Canvas/WebGL)
```

**Основной поток данных**:
1. Браузер загружает HTML → подключает Mapbox GL JS библиотеку
2. `script.js` инициализирует карту с кастомным стилем
3. Mapbox загружает векторные тайлы с удаленного источника (mapbox-streets-v8)
4. Стиль применяется к загруженным данным (фильтры, цвета, слои)
5. Карта рендерится в Canvas/WebGL и отображается в `#map` контейнере

**State Management**: Не используется (проект слишком простой, состояние управляется встроенными механизмами Mapbox GL JS)

## Directory Map

```
Map/
│
├── index.html                  # Точка входа: базовая HTML структура
│                                 Подключает Mapbox GL JS и локальные стили
│
├── script.js                   # ✅ JavaScript логика инициализации
│                                 - Функция initializeMap()
│                                 - Mapbox access token (placeholder)
│                                 - Инициализация карты (new mapboxgl.Map)
│                                 - Загрузка кастомного стиля
│
├── style.css                   # Глобальные стили для fullscreen карты
│                                 Устанавливает прозрачный фон и размеры
│
├── mapbox_style.json           # ⭐ Кастомный Mapbox Style Specification
│                                 Определяет layers (background, water, admin)
│                                 Источник данных: Mapbox Streets v8
│
├── test.html                   # 🧪 TDD Test Suite
│                                 Автоматические тесты для проверки:
│                                 - Инициализации карты
│                                 - Загрузки стиля
│                                 - Наличия всех слоев
│
├── README.md                   # 📖 Документация проекта
├── SETUP_INSTRUCTIONS.md       # 🚀 Инструкция по настройке Mapbox токена
├── .gitignore                  # Игнорируемые файлы (node_modules, .env)
│
├── package-lock.json           # Dependency lock file (Node.js ecosystem)
│
└── node_modules/               # npm зависимости:
    ├── d3-geo/                   - D3.js для географических расчетов
    ├── node-fetch/               - HTTP клиент для серверных запросов
    └── ...                       (вспомогательные утилиты для геоданных)
```

### Ключевые директории и их назначение:

- **Root (`/`)**: Все файлы находятся в корне - простая flat-структура для минималистичного проекта
- **`node_modules/`**: Установленные npm пакеты (`d3-geo`, `node-fetch`) - вероятно, для генерации/обработки геоданных на стороне сервера или в билд-процессе
- **`.cursor/docs/`**: Документация проекта (включая этот файл)

## External Services

### 1. **Mapbox GL JS API**
- **Назначение**: Рендеринг векторных карт, обработка тайлов, интерактивность
- **Версия**: v3.1.2
- **CDN**: `https://api.mapbox.com/mapbox-gl-js/v3.1.2/`
- **Требования**: Необходим Mapbox Access Token (должен быть в `script.js`)

### 2. **Mapbox Vector Tiles**
- **Источник данных**: `mapbox://mapbox.mapbox-streets-v8`
- **Тип**: Vector tiles с географическими данными (страны, водные объекты, административные границы)
- **Использование**: Source для слоев в `mapbox_style.json`

### 3. **Mapbox Fonts API**
- **Назначение**: Загрузка шрифтов для текстовых меток (если будут добавлены)
- **Endpoint**: `mapbox://fonts/mapbox/{fontstack}/{range}.pbf`

## Dependencies

### Runtime Dependencies:
- **Mapbox GL JS** (v3.1.2) - CDN, основная библиотека для карты

### Development/Build Dependencies (npm):
- **d3-geo** (^3.1.0) - Геопространственные расчеты и проекции
- **node-fetch** (^3.3.2) - HTTP запросы для серверных операций

## Technical Notes

### ✅ Исправленные проблемы:
- ✅ **`script.js` создан** - полная реализация инициализации карты
- ✅ **TDD тесты** - написан test.html с 6 автоматическими тестами
- ✅ **Документация** - добавлены README.md и SETUP_INSTRUCTIONS.md
- ✅ **Безопасность** - создан .gitignore для защиты чувствительных данных

### ⚠️ Требует настройки:
- **Mapbox Access Token** - пользователь должен получить токен на https://account.mapbox.com/
- Инструкции в файле `SETUP_INSTRUCTIONS.md`

### Технологический стек:
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Mapping**: Mapbox GL JS v3.1.2 (WebGL-based rendering)
- **Data Processing**: D3.js (geo utilities)
- **Styling**: Custom Mapbox Style Specification (v8)
- **Testing**: Custom TDD framework (встроенный test runner)

### Методология разработки:
- **TDD (Test-Driven Development)**: Проект следует строгому TDD подходу
  1. 🔴 RED: Написаны тесты (должны падать)
  2. 🟢 GREEN: Минимальная реализация для прохождения тестов
  3. 🔵 REFACTOR: Оптимизация кода после прохождения тестов

### Рекомендации для расширения:
1. ✅ ~~Добавить `script.js` с инициализацией карты~~ (завершено)
2. ✅ ~~Создать `.gitignore` для защиты токенов~~ (завершено)
3. Добавить слой маркеров/точек на карту (GeoJSON source)
4. Добавить интерактивные элементы UI (zoom controls, search bar)
5. Настроить build process (Vite/Webpack) для bundling
6. Добавить TypeScript для type safety
7. Интегрировать с реальным бэкендом для динамической загрузки данных

